{
  "name": "codedparts-tools",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "rm -rf dist && vite build",
    "preview": "rm -rf dist && vite build && vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.10",
    "vite-plugin-html": "^3.2.2",
    "vite-plugin-html-minifier": "^1.0.5"
  }
}
